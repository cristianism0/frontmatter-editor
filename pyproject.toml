[project]
name = "metadatamd"
version = "0.1.0"
description = "Leitor e Alterador de Metadata de arquivos markdown."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.0.1",
]
