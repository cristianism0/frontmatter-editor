[project]
name = "metadatamd"
version = "0.1.0"
description = "Leitor e Alterador de frontmatter de arquivos markdown."
readme = "README.md"
requires-python = ">=3.14"
[tool.pytest.ini_options]
pythonpath = [
  "."
]
dependencies = [
    "pytest>=9.0.1",
    "pytest-mock>=3.15.1",
]
